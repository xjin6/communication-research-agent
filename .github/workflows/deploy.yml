name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  schedule:
    # Every day at 08:00 UTC — auto-sync comm-agent changes
    - cron: "0 8 * * *"
  workflow_dispatch: # Manual trigger

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Fetch latest skills from comm-agent
        run: node scripts/fetch-skills.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.COMM_AGENT_WEB_READ }}

      - run: npx tsc -b && npx vite build

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - id: deployment
        uses: actions/deploy-pages@v4
